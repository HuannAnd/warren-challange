const devices = {
  mobile: '640px',
  tablet: '768px',
  computer: '1024px'
}

// const mediaQueries = {
//   sm: `(max-width: ${devices.mobile})`,
//   md: `(max-width: ${devices.tablet})`,
//   lg: `(min-width: ${devices.computer})`

// }

const mediaQueries = {
  sm: "(max-width: 1023px)",
  lg: "(min-width: 1023px)"
}

export { mediaQueries }